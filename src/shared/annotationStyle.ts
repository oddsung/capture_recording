/**
 * Rendering constants shared by the editor (react-konva) and the flattener
 * (SVG via sharp) so thumbnails/exports match what the editor shows.
 */

/** Konva `Line.tension` used for freehand pen strokes. */
export const PEN_TENSION = 0.35

/** Arrow head length and base width, as a multiple of the stroke thickness. */
export const ARROW_HEAD = 2.5
